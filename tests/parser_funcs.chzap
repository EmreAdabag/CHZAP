/* CHZAP functions */

int add(int a, int b) {
    return a + b;
}

int sum(int a, int b, function(int, int) -> int f) {
    return f(a, b);
}

function(void) -> int func_decl;

int main() {
    int a;
    int y;
    int x;
    float z;
    bool k;
    const int l;
    char c;
    int[] arr;
    function(int, int) -> int f;

    a = 3;
    y = 5 / 2.0;
    x = 5 * 2  + 1;
    z = x + y ; // int + float
    z = ( x + y ) * 2;
    k = x < 5;
    k = false;
    k = true;
    c = 'c';
    j = a + y + x;
    s = sum(a, b, function(int x, int y) -> int { return x + y; });
}
// there must be a new line at the end of file
