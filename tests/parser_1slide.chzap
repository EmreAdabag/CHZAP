/* CHZAP in one-slide */

function collatz(x):
	if x == 1:
		return 1
	else if x % 2 == 0:
		return x / 2
	else:
		return 3 * x + 1

// anonymous & higher-order functions
twice = (f, x):	
	return f(f(x))

// type inference 
a = 3
twice(collatz, a)		// 5

// compilation error
y = 5 / 2.0

// there must be a new line at the end of file
